export enum RoleType {
  OrgAdmin = "org_admin",
  BodyAdmin = "body_admin",
  Proposer = "proposer",
  Approver = "approver",
  Vetoer = "vetoer",
  Executor = "executor",
  EmergencyOperator = "emergency_operator"
}
